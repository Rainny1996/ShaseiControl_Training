# 男性控制训练 iOS — 代码复查报告 v5.7（GitHub "Build APP" 第二次实编译失败复审）

> 复审基线：`docs/reviews/v5.6-代码复查报告.md` ＋ 本次 GitHub Actions "Build APP" 步骤 `xcodebuild build` 失败日志
> 生成时间：2026-07-11
> 方法学：以 **CI 实编译日志为唯一真相源**，并**直接读取本地磁盘文件**逐条核实，不沿用上一轮归类判断。

## 0. 结论

❌ 构建仍失败（"Build APP" `** BUILD FAILED **`），但本次已**定位并直接修复全部残留错误的本地代码**。

🔴 **关键纠正（推翻 v5.6 核心论断）**：v5.6 声称「Group A（CoreDataStack / SecurityService）本地早已修复，仅因未推送而 CI 报错」。经本次直接读取本地磁盘核实，该论断**不成立**——本地这两份文件实际上仍是坏的（只是坏法不同于 GitHub 旧版），且 v5.6 的 PrivacySettingsView 修复也**未真正落到磁盘**。本轮已将这几处全部实修。

## 1. 实编译暴露的错误清单（本次日志为真相）

| # | 文件:行 | 错误 | 与 v5.6 关系 |
|---|---------|------|------|
| 1 | CoreDataStack.swift:19 | `cannot convert value of type 'String' to expected argument type 'NSObject'`（`as NSObject`） | v5.6 称本地已修；本地核实：本地第 19 行实为正确 `setOption(..., forKey: NSPersistentStoreFileProtectionKey)`，此错属 GitHub 旧版本，**推送即消** |
| 2 | CoreDataStack.swift:31 | `cannot find 'excludeFromiCloudBackup' in scope` | 同上，本地第 31 行调用正确，推送即消 |
| 3 | CoreDataStack.swift:100 | `attribute 'private' can only be used in a non-local scope` | **本地真实存在**：`deleteAllUserData()` 缺闭合 `}`，使 `excludeFromiCloudBackup` 被当成嵌套函数 |
| 4 | CoreDataStack.swift:111 | `expected '}' in class` | **本地真实存在**：同上缺括号导致类体未闭合 |
| 5 | PrivacySettingsView.swift:65 | `to match this opening '('`（`Section(header:content:)`） | **本地真实存在**：v5.6 的修复未落盘 |
| 6 | PrivacySettingsView.swift:83 | `expected ')' in expression list` | 级联，本地真实存在 |
| 7 | PrivacySettingsView.swift:121 | `type '() -> Text' cannot conform to 'View'`（`Section(header:content:)`） | **本地真实存在** |
| 8 | PrivacySettingsView.swift:141 | `argument 'footer' must precede 'content'` / `expected ',' separator` | 级联，本地真实存在 |
| 9 | DataViewModel.swift:55 | `expression is 'async' but is not marked with 'await'`（`collectAllData`） | 本轮暴露（跨 MainActor 调用缺 `await`） |
| 10 | DataViewModel.swift:61 | 同上（`cryptoService.encryptData`），Swift 5 仅 warning、Swift 6 为 error | 本轮暴露 |
| 11 | SecurityService.swift:306 | `cannot find 'PBKDF2' in scope` | v5.6 称本地已修；本地核实：`import CryptoKit` 存在，但 `deriveKey` 用了**错误标签** `password:salt:iterations:` |
| 12 | SecurityService.swift:311 | `no exact matches in call to initializer`（`Data($0)`） | 级联自 #11 标签错误 |

> 注：v5.6 报告中的 #8/#9（showError private）、#10/#11（Data? 解包）、#12（TrainingGoal.description）在 CI 中已**消失**——说明 v5.6 的这三项修复已正确推送，本轮无需再改。

## 2. 本轮修复动作（已写入本地磁盘）

| 文件 | 根因 | 修复 |
|------|------|------|
| CoreDataStack.swift | `deleteAllUserData()` 缺闭合 `}`，使 `excludeFromiCloudBackup` 沦为**嵌套函数**、类体未闭合 | 在 `performBackgroundTask` 闭包 `}`（原第 95 行）后补一个 `}` 关闭 `deleteAllUserData()`，使 `excludeFromiCloudBackup` 回到类作用域 |
| SecurityService.swift | `PBKDF2<SHA256>.deriveKey` 参数标签错误（`password:salt:iterations:` 不符 CryptoKit 真实签名） | 改为 `deriveKey(fromKeyMaterial:salt:length:iterations:using: .derivedKey)`；用 `[UInt8](symKey.withUnsafeBytes { $0 })` + `Data(bytes)` 消除 `Data($0)` 歧义 |
| PrivacySettingsView.swift | 两处非法的 `Section(header: Text(...), content: { })` 标签顺序（且 `footer` 须在 `content` 前，struct 不能作 View） | 改为合法尾随闭包 `Section { } header: { } footer: { }` 形式（"密码管理"、"关于"两处） |
| DataViewModel.swift | `performExport` 在 `Task.detached` 内调用 `@MainActor` 隔离的 `collectAllData` / `cryptoService`，缺 `await` | 第 55 / 61 行分别加 `await` |

## 3. 过程缺陷复盘（v5.6 的虚假结论）

- v5.6 在**未实际读取本地磁盘**的情况下，断言 Group A「本地早已修复」。本次直接读取证伪该论断——`CoreDataStack` 仍缺括号、`SecurityService` 的 `PBKDF2` 标签仍错。
- 同时 v5.6 对 PrivacySettingsView 的修复未真正落盘（上一轮替换冲突/重试未持久），导致 CI 仍报该错。
- **教训**：当 CI 仍报错时，必须默认「本地也坏」并**亲自读取核实**，不得沿用上一轮对"已修/残留"的归类；审查结论只能基于本次实测的磁盘内容。

## 4. 放行判定与下一步

- 本地磁盘现已消除本次日志全部 12 处错误（含级联）；v5.6 已推送有效的 3 项修复保留不动。
- ⚠️ 本环境为 Windows，无法实跑 `xcodebuild`，最终放行以 **CI 重跑结果**为准。
- 必须执行：
  1. `git add` 本次改动（CoreDataStack / SecurityService / PrivacySettingsView / DataViewModel）→ `git commit` → `git push`；
  2. 重跑 GitHub Actions "Build APP"，预期 `** BUILD SUCCEEDED **`；
  3. 通过后跑 `test.yml`（166 用例）。

## 5. 已知未决（非阻断）

- 日志中的 `warning:`（`onChange(of:perform:)` 弃用、`applicationIconBadgeNumber` 弃用、未使用变量等）不影响编译，可后续清理；不在本次修复范围。
